import { ChildrenProps } from '@/domain/types'
import { Box } from '@mui/material'

const Header: React.FC<ChildrenProps> = ({ children }) => {
  return <Box display="flex"></Box>
}

export { Header }
